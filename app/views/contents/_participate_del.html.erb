<%= link_to(participation_path(content, content_id: content.id), method: :delete, remote: true) do %><span class="text-muted">参加した </span><span id="participating_users"><%= content.participating_users.count %></span><% end %>