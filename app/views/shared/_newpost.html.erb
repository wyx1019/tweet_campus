<%= form_with(model: @micropost, local: true) do |f| %>
    <%= f.text_area :content, class: 'form-control newpost-area', placeholder: "250文字以内にテキスト入力してください", size: "40x12" %>
    <%= f.file_field :image %>
    <%= f.submit "投稿する", class: "btn" %>
<% end %>