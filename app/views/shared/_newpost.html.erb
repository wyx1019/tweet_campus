<%= form_with(model: @micropost, local: true) do |f| %>
    <%= f.label :投稿を入力してください %>
    <%= f.text_area :content, class: 'form-control', placeholder: "250文字以内にテキスト入力してください", size: "40x12" %>
    <%= f.submit "投稿する", class: "btn btn-primary" %>
<% end %>